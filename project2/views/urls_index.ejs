<!DOCTYPE html>
<html lang= "en">
<head>
  <title>Tiny App</title>
</head>
<body>
    <h1> List of URLS </h1>
      <ul>
        <% for(url in urls) { %>
          <li>
            <%= `[URLs]: ${urls[url]},   [Shortened-URL]: ${url}` %>
          </li>
        <% } %>
      </ul>
</body>
</html>